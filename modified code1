  def start_timer(self):
        try:
            seconds = int(self.timer_entry.get().strip())
            if seconds <= 0:
                raise ValueError
        except ValueError:
            messagebox.showerror("Error", "Enter a valid number of seconds!")
            self.visual_feedback(success=False)
            return
      
